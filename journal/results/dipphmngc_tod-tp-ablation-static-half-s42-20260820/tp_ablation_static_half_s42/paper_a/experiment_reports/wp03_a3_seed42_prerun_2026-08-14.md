# WP03 A3 Pre-Run: Matched Seed-42 Fill

**Status:** `SMOKE_PASSED_FIRST_SHARD_READY`  
**Authorization chain:** A1 canonicality pass → `PL-003` frozen by owner on
2026-08-14 → this two-shard pre-run package  
**Scope:** validation-only; no final-test access, no ablation, no retry sweep

## Purpose

Seed 42 is the only missing homogeneous pair in the WP02-hash proposed-method
matrix. The historical WP01 pilot cannot fill it because it used a different
trainer hash. These two new runs are the sole A3 workload.

| Kernel | Invocation label | Canonical method | Seed | Account |
|---|---|---|---:|---|
| `wp03-a3-alw-canonical-s42` | `alw_canonical` | `alw_canonical` | 42 | `pptlyn11` |
| `wp03-a3-sa-alw-full-s42` | `sa_alw_full` | `sa_alw_canonical` | 42 | `pptlyn11` |

The two kernels run serially on the same verified account. On 2026-08-14 the
owner authorized private, immutable replicas because the source owner had only
0.74 GPU hours and no other account held dataset ACLs. `pptlyn11` now owns
verified private replicas with the same slugs. The account switch changes only
the metadata dataset owner; it does not change the packaged kernel source,
trainer hash, split hashes, seed, budget, or evaluation contract.

## Frozen contract

- Accelerator request: `NvidiaTeslaT4`; kernel exits before training unless a
  Tesla T4 is observed.
- Inputs: private `pptlyn11/tinyperson-wp01-a1` and
  `pptlyn11/paper-a-code-wp02`; both were live-listed after private replica
  publication. The T4 mount/model-init smoke is downloaded under
  `.runtime/kaggle/wp03/a3_seed42/pptlyn11/outputs/smoke_v1/`.
- Trainer SHA-256: `7c05831cbc544b84926694ecdd85159a9ac85ee557a7dc6894bebcfaed2b5d03`.
- PL-001 split hashes: train `5bea11d2...f026b`, validation
  `31d67f94...ab27`; 8 epochs, batch 4, horizontal-flip-only augmentation,
  fixed IoU-NMS, validation COCO-AP checkpoint selector, and frozen linear
  P10/P90 schedule.
- Output tag: `wp03_a3`; expected run directories are
  `runs/wp01_pilot_<label>__seed42__wp03_a3/`.

## Capacity and acceptance

At preparation time, `ngquangnht` had only 0.74 GPU hours remaining, while
each eight-epoch run historically needs about 4–5 hours. `pptlyn11` had 29.95
hours and no active kernel at the quota audit. Its required mount/GPU/model-init
smoke passed on Tesla T4 ×2 before any training kernel is pushed. No partial
run is treated as evidence.

Each row is accepted only after terminal status, downloaded logs and small
artifacts, `best.pt`, epoch metrics, detections, package audit, strict reload,
primary endpoint tolerance, and evaluator/prediction checks all pass. A
`COMPLETE` status alone is not a result. The post-run report must then build
the six-method × three-seed matrix and the paired original-image bootstrap.

## Local preparation check

```powershell
.\.venv-cuda\Scripts\python.exe paper_a\tools\prepare_wp03_a3_seed42.py --write
```

The generated kernels and `run_plan.json` live under
`.runtime/kaggle/wp03/a3_seed42/`.
